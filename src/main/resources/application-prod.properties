# ==========================================================
# CONFIGURACIÓN GENERAL - PRODUCCIÓN
# ==========================================================
spring.application.name=api_salud
server.address=0.0.0.0
server.port=${PORT:8090}

# ==========================================================
# CONEXIÓN A BASE DE DATOS (VIA VARIABLES DE ENTORNO)
# ==========================================================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.type=com.zaxxer.hikari.HikariDataSource

# ==========================================================
# CONFIGURACIÓN DE CORREO (CLARO EMPRESAS)
# ==========================================================
spring.mail.host=smtp.claroempresas.com.pe
spring.mail.port=587
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ==========================================================
# POOL DE CONEXIONES (HIKARI) - OPTIMIZADO PARA NEON
# ==========================================================
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.connection-test-query=SELECT 1

# ==========================================================
# LOGGING PROFESIONAL
# ==========================================================
logging.level.root=INFO
logging.level.com.api_salud.api_salud=INFO
logging.level.org.springframework=WARN

server.compression.enabled=true
server.compression.mime-types=application/json,text/plain,text/javascript
# =======================================================================
# CONFIGURACIÓN DE ALMACENAMIENTO LOCAL (ENTORNO DE DESARROLLO - WINDOWS)
# =======================================================================
# Ruta física base para las rúbricas e imágenes de las firmas de los médicos
#app.storage.ruta-firmas=D:\\08_PROYECTOS\\STORAGE\\firmas\\
# Ruta física base donde se guardarán los PDFs generados de la HCE
#app.storage.ruta-pdfs=D:\\08_PROYECTOS\\STORAGE\\pdfs\

# =======================================================================
# CONFIGURACIÓN DE ALMACENAMIENTO (MODO DUAL)
# =======================================================================
#app.storage.provider=${STORAGE_PROVIDER:LOCAL}
#app.storage.provider=R2

# LOCAL (Windows)
#app.storage.local.root-path=D:/ARCHIVO_DIGITAL

# RUTAS (Esto es lo que te faltaba para que storageConfig.getPath() no sea null)
#app.storage.path.historias=/{empresa}/historias_{empresa}/{paciente}/atencion_{atencion}_{empresa}.pdf

# GOOGLE CLOUD STORAGE
#app.storage.gcs.bucket-name=tu-bucket-nombre
#app.storage.gcs.project-id=tu-proyecto-id

app.storage.provider=R2

# LOCAL (Windows)
app.storage.local.root-path=D:/ARCHIVO_DIGITAL

# RUTAS
#app.storage.path.historias=/{empresa}/historias_{empresa}/{paciente}/atencion_{atencion}_{empresa}.pdf

# Carpeta temporal para borradores (se puede limpiar mediante un Job automático diariamente/semanalmente)
app.storage.path.borradores=/{empresa}/borradores/{paciente}/atencion_{atencion}_borrador.pdf

# Carpeta definitiva congelada para documentos firmados
app.storage.path.historias=/{empresa}/historias/{paciente}/atencion_{atencion}_{empresa}.pdf

# MODO CLOUDFLARE R2 (S3 API)
app.storage.r2.endpoint=${CLOUDFLARE_R2_ENDPOINT:https://f9b694bb8050c5669bb804cde1824867.r2.cloudflarestorage.com}
app.storage.r2.access-key-id=${CLOUDFLARE_R2_ACCESS_KEY_ID:db6ee49c4f385e7ea4d0a5c715dc5324}
app.storage.r2.secret-access-key=${CLOUDFLARE_R2_SECRET_ACCESS_KEY:c9c9997ad769a74d2333f11d75584c31ca7c557bd40c413586d1b58327c6e747}
app.storage.r2.bucket-name=${CLOUDFLARE_R2_BUCKET_NAME:igm-salud-historias-clinicas}
app.storage.r2.public-url-base=${CLOUDFLARE_R2_PUBLIC_URL_BASE:https://pub-xxxxxxxx.r2.dev}

